providers = ["python"]

[variables]
    PORT = "8000"

[phases.install]
    cmds = ["pip install -r requirements.txt"]

[start]
    # Changed 'main:app' to 'api:app' to match your file name
    cmd = "uvicorn api:app --host 0.0.0.0 --port 8000"
